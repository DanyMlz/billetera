/*
package com.example.Simon;


public class SimonC {

 public int diasObjetivos;
    public int diasPasadosCorrectos;
    public int diasPasadosIncorrectos;
    public int mbTotales;

    public int diasFaltantes(int diasObjetivo, int diasPasadosCorrectos, int diasPasadosIncorrectos){

        if (diasObjetivo <= 0) {
            throw new ArithmeticException("El objetivo minimo es de 1 dia");
        }

        if (diasPasadosCorrectos < 0) {
            throw new ArithmeticException("dias pasados no pueden ser menor que 0");
        }

        if (diasPasadosIncorrectos < 0) {
            throw new ArithmeticException("dias pasados no pueden ser menor que 0");
        }


        if (diasObjetivo == diasPasadosCorrectos && diasPasadosIncorrectos==0) {
            return 0;
        } else {

            if (diasObjetivo == diasPasadosIncorrectos + diasPasadosCorrectos) {
                throw new ArithmeticException("Objetivo no alcanzado");
            } else {
                return diasObjetivo = diasObjetivo - (diasPasadosCorrectos + diasPasadosIncorrectos);
            }
        }
    }
}
*/
// xd
//simon?
//JOHAN:DMarcelito